
-- Automatically use expanded display when necessary.
\x auto

-- Improve appearance of results.
\pset linestyle unicode

-- Display results compactly and not line-wrapped by default.
\pset format unaligned

-- Display nulls differently to empty strings.
\pset null [NULL]

-- Show time queries take.
\timing
