
# Note: need to initially run `./sync` for this session to be available.

name: underware-secondary

windows:
  - underware:
      layout: even-horizontal
      root: <%= ENV.fetch('ALCES') %>/underware
      panes:
        - rspec

  - metalware:
      layout: even-horizontal
      root: <%= ENV.fetch('ALCES') %>/metalware
      panes:
        - rspec
