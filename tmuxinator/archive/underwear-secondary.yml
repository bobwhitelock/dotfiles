
# Note: need to run `sync` for this session to be available.

name: underwear-secondary

windows:
  - underwear:
      layout: even-horizontal
      root: <%= ENV.fetch('ALCES') %>/underwear
      panes:
        - rspec

  - metalware:
      layout: even-horizontal
      root: <%= ENV.fetch('ALCES') %>/metalware
      panes:
        - rspec
