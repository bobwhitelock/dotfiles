
# Note: need to run `sync` for this session to be available.

name: gotv-dashboard-secondary

windows:
  - tests:
      root: <%= ENV.fetch('SRC') %>/CampaignLab/gotv-dashboard
      panes:
        - rspec
