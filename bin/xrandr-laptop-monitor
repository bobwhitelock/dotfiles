#!/bin/bash
set -euo pipefail
IFS=$'\n\t'

xrandr \
    --output DP1 --mode 1920x1080 --pos 0x0 --rotate normal \
    --output DP2 --off \
    --output HDMI1 --off \
    --output HDMI2 --off \
    --output VIRTUAL1 --off \
    --output eDP1 --off

al-polybar-session &
